import { User } from '../models/user.model';

export interface AppState {
  user: User | null;
  toolbarTitle: string;
}

export interface AppState {
    user: User | null;
    toolbarTitle: string;
  }